<%= form_for @event, :html => { :class => "form-horizontal event" } do |f| %>

  <% if @event.errors.any? %>
      <div id="error_expl" class="panel panel-danger">
        <div class="panel-heading">
          <h3 class="panel-title"><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h3>
        </div>
        <div class="panel-body">
          <ul>
          <% @event.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
          </ul>
        </div>
      </div>
  <% end %>

  <div id="eventname">
    <%= f.text_field :name, placeholder: "name" %>
  </div>

  <div id="clock"></div>

 <!--  <%= f.label :start %>    -->
    <%= f.hidden_field :start %>    

<!--   <%= f.label :duration %>   --> 
    <%= f.hidden_field :duration %>

<!--   <%= f.label :day %>   --> 
    <%= f.hidden_field :day %>

    <%= f.hidden_field :eventype %>    

 

  <!--div id="eventype">
    <%= f.select(:eventype, ([['DailyLIfe', '#0f2'], ['MustDo', '#d33',], ['WouldLike', '#26f']])) %> 
  </div-->



  <div class="submit">
    <%= f.submit :id => 'submit', :class => 'btn btn-default' %>
  </div>

  <script src="/assets/d3arc.js"></script>

<% end %>